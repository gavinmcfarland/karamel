--iterate(rules) rule--
	--if(@this.name === 'default') @down() modifier--
.{{@rule:abbr}} {
		--# else--
.{{@rule:abbr}}-{{@modifier:name}} {
	--/if @down() prop--
	{{@prop:name | kebabcase}}: {{@prop:value}};
	--@up()--
}
--/iterate--
